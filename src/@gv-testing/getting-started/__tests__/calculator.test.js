
describe('Test Calculator', () => {

	test('add should a + b', () => {
	});

	test('sub should a - b', () => {
	});

	test('mul should a * b', () => {
	});

	test('div should a / b', () => {
	});
});
